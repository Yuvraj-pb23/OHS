# Open Hand Solution (OHS)

This repository contains the complete infrastructure, containerization, and automated deployment configuration for OHS.

## Getting Started

### Local Development
```bash
docker-compose --profile dev up --build
```

### Production Setup
```bash
docker-compose --profile prod up -d --build
```
